import github from 'wiki-now/utils/github';
import { module, test } from 'qunit';

module('Unit | Utility | github');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = github();
  assert.ok(result);
});
